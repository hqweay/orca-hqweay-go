# 样式清除 (Remove Style)

快速清理笔记内容的格式。

## 功能特性

- **移除行内样式**：清除加粗、高亮等富文本样式。
- **移除链接**：将链接转换为纯文本。
- **移除空格**：将空行（且无子节点）删除。
- **转换为自动标题**：将笔记内的各种级别的标题统一转为自动层级标题。

## 设置项配置

可在子插件设置面板中自定义选择启用哪些清理功能：
- **清除内联样式**（默认启用）
- **清除链接样式**（默认启用）
- **清除空行**（默认启用）
- **转换为自动标题**（默认启用）

禁用后的功能将不会在顶部栏菜单及聚合动作菜单中显示。

