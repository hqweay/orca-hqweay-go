# Changelog

## 4.0.0

### Major Changes

- c195739: **4.0 来了！这版主打 AI，让 Orca 会替你干活。**

  ### 用 AI 写脚本，不会写代码也能玩转

  - 描述一下你想要的效果，AI 就能帮你写脚本：改界面样式（纯 CSS）、执行一次性操作、做常驻侧边栏挂件（比如专注时钟）
  - 三个入口随手可用：设置页「AI 生成脚本」、编辑器斜杠命令「AI 生成脚本」、`#脚本` 代码块右键「用 AI 改写」
  - 生成结果先在预览面板展示给你看（含用途说明），确认后才插入，`autoRun` 要你手动勾选——AI 只建议，决定权在你
  - AI 不确定用什么命令时，会先查本机内置的官方命令文档，不会瞎编命令名

  ### 认识挂件：常驻侧边栏的实用小工具

  - **挂件（Widget）**是常驻在侧边栏「更多」tab 的小工具：像番茄钟那样，聚焦后整栏展示，随时可用；失焦后继续运行，回到侧边栏就能看到实时状态（比如还剩几分钟）
  - 一次生成，长期受用：AI 能帮你做专注时钟、全局字体颜色、快捷面板等各类挂件
  - 挂件写法升级为**纯函数渲染**：生成的代码更干净、更可预期，运行更稳定；「小番茄钟」等模板同步迁移
  - **内置「全局字体颜色」挂件模板**：色盘/自定义颜色一键换全应用文字色，选择本地保存、卸载自动还原
  - 脚本清理升级为显式 `$inject.onUnload`：加载/卸载开关可靠稳定，AI 生成的脚本也能干净卸载，不留残留

  ### 脚本与模板，插入即用

  - 设置页模板库新增常用模板，一键插入：**禅模式**（沉浸式写作，聚焦块高亮、其余内容虚化模糊）、全局字体颜色、块嵌入子项、快捷打标签、侧边栏切换布局、小番茄钟
  - 插入模板即**自动注册生效**：脚本立即出现在侧边栏「更多」tab，快捷键也立刻可用，无需手动「重新注入」

  ### 用 AI 做嵌入，一句话生成实时小组件

  - 输入网址或一句话描述，AI 就能生成嵌入内容；编辑现有嵌入时也可「AI 改写」
  - AI 生成的嵌入可以查询笔记数据（设置里开启权限即可），做出数据看板、过滤列表这类实时小组件

  ### 等待不再干着急

  - AI 生成过程中会显示转圈动画、已等待计时和进度条——等多久一眼可见，不用猜是不是卡了

### Patch Changes

- 521f045: Arc 侧边栏：新增「在「更多」tab 显示最近访问」开关。

  - 设置页开启后，侧边栏「更多」tab 会列出最近访问的块（与 Arc 侧边栏共用同一份数据，实时同步），点击即可跳转回对应块
  - 最近访问的**追踪不再依赖 Arc 面板是否打开**：插件加载期间持续记录，Arc 面板关闭时 tab 里的最近访问也照常更新
  - 最近访问列表底部提供「清空最近访问」入口（与 Arc 侧边栏共用同一份历史，一起清空）
  - 未开启时行为不变；开关运行时切换即时生效，无需重启

- d74441e: 嵌入视图插件：新增 AI 生成嵌入功能。

  - 输入网址或一句话描述，AI 就能生成嵌入内容，确认后插入到当前块后面
  - 编辑嵌入时多了「AI 改写」按钮：基于当前内容按你的要求生成草稿，确认后再保存
  - 未配置 AI 或生成失败时会给出提示，不会留下空白或损坏的嵌入

- 1a32c38: 嵌入视图插件：AI 生成的嵌入可以查询笔记数据，做出实时小组件（如数据看板、过滤列表）。

  - 查询前需在设置页开启「查询权限」，默认仅 AI 生成的嵌入可查询，也可关闭
  - 单次查询结果数量有限，避免一次拉取过多数据
  - 修复两个常见问题：AI 生成偶尔失败、嵌入一直卡在「加载中…」
  - 嵌入内容自带内边距，不再紧贴块边缘
  - AI 生成的嵌入会记住提示词，点「重新生成」时直接复用，方便微调

- dd4bcb0: 脚本注入插件改进：

  - 手动运行的脚本也可以自动清理副作用，重复运行不会叠加残留内容
  - 只有 CSS / JavaScript 代码块才会被当作脚本，其他语言的代码块不再被误执行
  - 修复重复点击「重新注入」可能导致按钮重复注册的问题
  - 设置页新增显示模式选项，可调整顶部栏脚本入口的展示方式

- fbb3b83: 脚本注入插件：顶部栏的脚本菜单支持分组。在 `#脚本` 标签属性中填写分组名（如 Theme、Tools），脚本会按组折叠展示；没填分组名的脚本保持平铺。
- f94a9c1: 脚本注入插件新增「模板库」，内置三个常用模板，一键插入到今日日志：

  - **块嵌入子项**：块被嵌入展示时仅展示子内容
  - **快捷打标签**：一键给当前块或选中的块打上多个标签
  - **侧边栏布局**：在侧边栏「更多」tab 里注册「布局」组，点击名称即可切换布局（思路参考 [Orca-layout-manager](https://github.com/luckyoubest2/Orca-layout-manager)）

  其他改进：

  - 插入的代码块自动折叠成一行并带标题，日志更整洁
  - 模板默认不启用，点击代码块即可切换，也可设置为启动时自动加载
  - 脚本里可以调用 AI 对话，自动生成标签属性值
  - `$inject` 新增 `registerSidebarGroup` 与 `setSidebarTabTitle`：侧边栏新增共享「更多」页签（懒加载，按需出现），任何脚本/插件都能在里面注册文件夹组、组内条目，或注册时钟这类整栏挂件；组支持折叠，聚焦时列表只展示该组内容占满整栏
  - `#脚本` 标签新增 `sidebar` 属性：把脚本作为**启动器条目**（headbar 式列表）列出到侧边栏「更多」tab——一行 `[图标] [脚本名]`，点标题加载/卸载切换，点左侧图标聚焦该脚本视图（脚本名 + 运行/卸载按钮）；刷新时只列出、不执行（不自动执行，要自动执行另勾 `autoRun`）；纯属性驱动，脚本内无需调用侧边栏 API；填了 `group` 属性按 group 值归入文件夹，否则归入默认「脚本」文件夹（设置里可改）；`displayName` 自定义脚本名、`icon` 自定义图标；条目由插件自动跟踪，下次「重新注入」按最新属性重建
  - 以后新增模板只需在模板目录放一个文件
  - **AI 生成脚本**：设置页「AI 生成脚本」按钮 / 编辑器斜杠命令「AI 生成脚本」/ 任意 `#脚本` 代码块右键「用 AI 改写」三个入口；生成前可选形态（纯 CSS / JS 一次性 / JS 挂件 / 让 AI 判断）；预览面板展示代码与用途说明，`autoRun` 需显式勾选（AI 只能建议不能替用户决定）；改写保留标签与已有属性并自动重新注入；AI 不确定命令名时会通过 `query_api_docs` 工具查询本地 vendored 的官方命令文档（不联网、不编造命令名）

- 63c72bd: 脚本注入插件改进：

  - 常驻脚本（注册了清理函数，如「侧边栏布局」）可反复开关：从顶部栏 / 侧边栏 / 快捷键再次执行会卸载，而不是重复注入；再执行一次又恢复加载（清理函数现已改为显式 `$inject.onUnload`，见 4c50717）
  - 顶部栏脚本菜单里，已加载的脚本会带勾选并高亮显示
  - 右键菜单「对选中块执行」始终正常执行，不会被误当成卸载
  - 一次性脚本（如「快捷打标签」）每次执行照常运行，行为不变
  - 修复：加载「侧边栏布局」后，布局列表会显示在收藏等其他标签页界面的问题

- 59712c0: 脚本注入插件：侧边栏「更多」tab 升级为挂件展示台。

  - **改名**：插件更名为**「用户脚本 (User Scripts)」**（原「脚本注入」）；「注入标签」设置更名为「脚本标签」。内部标识（`#脚本` 标签、`$inject`、`lets-inject`）保持不变

  - **挂件 (Widget)**：脚本可用 `$inject.registerSidebarGroup` 的 `render` 模式注册自定义挂件——聚焦视图整栏渲染内容。新增「小番茄钟」模板（25 分钟专注 / 5 分钟休息，可开始/暂停/重置，失焦后计时继续）
  - **挂件快照**：挂件行内实时显示状态小字（如「12:34 · 专注中」），瞄一眼就知道在不在跑、剩多久
  - **启动器条目**：`sidebar` 属性脚本作为启动器行列出（点击 = 加载/卸载切换），行内状态点区分加载态，悬停出现「在笔记中定位」按钮一键跳转到代码块
  - **文件夹聚焦**：点文件夹标题进入聚焦整栏查看，点箭头折叠；文件夹图标改为代码风格
  - **折叠与排序持久化**：文件夹/组的折叠状态、顶层行的拖拽排序（组与文件夹整块可拖）都会保存，重启后保留
  - **统一文件夹机制**：`group` 标签属性同时控制启动器条目与挂件的文件夹归属，`displayName` 统一显示名——在属性面板改即可，无需改脚本代码；未填 `group` 时默认收进**注入标签同名的文件夹**（原「默认侧边栏文件夹」设置已移除，直接复用注入标签值）
  - **脚本元数据**：`$inject` 新增 `blockId` / `scriptName` / `scriptGroup`
  - **修复**：侧边栏里 emoji 图标渲染歪斜的问题（`<i>` 元素默认斜体 + 基线排列）

- d499586: 脚本注入插件：标签属性 `sidebar` 重命名为 `sidetool`（与 Orca 的 sidetool 机制命名一致）。若你在 `#脚本` 标签属性中配置过 `sidebar`，请改为 `sidetool`。

- 4c50717: 脚本注入插件：脚本清理契约升级为显式 `$inject.onUnload(清理函数)`。

  - 原来「在脚本末尾 `return` 一个清理函数」的方式，在 AI 生成的脚本（常被 IIFE 包裹）里会失效——`return` 留在函数内部，清理没被捕获；现在统一改为在脚本里调用 `$inject.onUnload(fn)`，行为明确可靠
  - 注册了清理的脚本可反复开关：从顶部栏 / 侧边栏「更多」tab / 快捷键再次执行 = 卸载（而非重复注入），再执行一次又恢复加载
  - 一次性脚本注册清理后也能变成可开关条目；未注册的一次性脚本每次执行照常运行
  - 「侧边栏切换布局」「小番茄钟」等模板已同步迁移到新契约

- 0c332ac: 脚本注入插件：新增「全局字体颜色」模板挂件。

  - 在侧边栏「更多」tab 注册「全局字体颜色」挂件：从色盘或自定义颜色选色，**实时改变整个应用的文字颜色**
  - 选择保存在本地，卸载后自动还原
  - 实现范式：document 事件委托 + localStorage 持久化（静态 HTML，不依赖 onFocus 容器引用，切 tab 重渲染后依然生效）——与番茄钟的 onFocus 范式对照

- 45ab5b9: 脚本注入插件：模板插入体验升级——**插入即自动生效**。

  - 插入模板时默认勾选 sidebar，启动器条目自动注册：脚本立即出现在侧边栏「更多」tab，无需手动「重新注入」即可点击加载/卸载
  - 带快捷键的模板插入后快捷键立即生效

- 2319cbf: 脚本注入插件：新增「禅模式」CSS 模板。

  - 沉浸式写作样式：隐藏头部工具、面板拖拽柄、块编辑器侧栏等干扰元素，当前聚焦块高亮显示，其余内容虚化模糊
  - 插入后自动注册，侧边栏「更多」tab 即可加载/卸载

- d411302: 修复侧边栏「更多」tab 里文件夹/组的折叠箭头方向（展开时朝下、折叠时朝右），与常见折叠控件一致。

## 3.7.6

### Patch Changes

- 54cab5c: 大纲导航面板和右侧边栏性能优化：减少无效重渲染，展开折叠及滚动操作更流畅
- d238d23: 脚本注入插件重构：支持快捷键绑定，代码结构拆分更易维护。现在可以为代码脚本设置全局快捷键（在标签属性中填写 shortcut），支持 ctrl+shift+k 等快捷键格式。

## 3.7.5

### Patch Changes

- 9898ac1: feat: replace pinyin-pro slug with MyMemory API translation + remove pinyin-pro dependency

## 3.7.4

### Patch Changes

- d06d6c4: lets-roam-sidebar: 收起内容时支持拖拽调整高度

  - 右侧边栏收起内容后，底部出现拖拽条，可自由调整内容区域高度
  - 调整后的高度会保存，关闭再打开依然保持
  - 拖拽条常亮显示，方便发现
  - 去掉了内容区域的边框，视觉更干净

- 6b43565: lets-inject: 简化脚本注入模型，统一通过 $inject 注入变量并提供官方示例

  - 移除 $inject.activeBlockId() 方法
  - $inject 只保留 targetBlockIds 字段，未来可扩展
  - 脚本内获取焦点块改用 orca.utils.getCursorDataFromSelection(window.getSelection())
  - 设置页面示例更新为 batchInsertText + cursor 模式，支持 Markdown 解析

## 3.7.3

### Patch Changes

- 插件设置页 headbar 模式选择器修复：用 renderCustomSettings() 替代 settingsComponent，自定义设置自动获得头栏模式控制

## 3.7.2

### Patch Changes

- 子插件设置页缺少 headbar 显示模式控制，通用修复：PluginSettings 支持 children prop，自定义设置组件自动带出头栏模式选择器

### Features

- 脚本注入 (lets-inject) 打磨完善
  - 恢复脚本自身右键运行命令，支持多块选择执行
  - 设置页新增折叠式使用指南，含完整示例
  - 注入/执行失败时显示具体错误信息和脚本名称
  - 注入完成通知显示脚本数量及名称

## 3.7.0

### Features

- 新增脚本注入插件 (lets-inject)：将代码块注入到 Orca UI 中，实现用户脚本/样式管理，提供瑞士军刀式的代码片段执行工具箱
  - CSS 注入：lang 设为 css，添加 #脚本 标签，启动时自动注入样式，刷新时自动移除
  - JS 自动注入：lang 设为 javascript，添加 #脚本 标签，启动时自动执行，支持 return 销毁函数实现刷新时干净卸载
  - 脚本自身运行：JS 脚本块右键直接 ▶ 运行，无需 menu 属性，返回值实时展示
  - 块菜单执行：标签属性勾选 menu 后，其他块的右键菜单出现「脚本」子菜单，脚本内通过 targetBlockIds 获取选中的块 ID（支持多选）
  - Async 支持：脚本内可直接使用 await（如 fetch、invokeEditorCommand 等 API），无需手动包裹 async 函数
  - 启用/禁用：通过 #脚本 标签的 enabled 属性独立控制每个代码块
  - 刷新按钮：headbar 提供刷新按钮，修改后点击全清全重来
  - 注入通知：注入完成时显示脚本数量及其名称（从代码块 cap 属性读取）
  - 错误详情：注入/执行失败时 toast 显示具体错误信息和脚本名称
  - 使用指南：设置页提供折叠式说明，含 CSS 注入、JS 销毁函数、手动执行完整示例

## 3.6.0

### Features

- 新增嵌入视图插件 (lets-embed-view)：支持在笔记中嵌入网页、HTML 代码和 Twitter/X 内容
  - 网址嵌入：输入网址即可直接显示网页内容，GitHub 等以往无法嵌入的网站也能正常展示
  - HTML 嵌入：直接编写 HTML 代码，实时预览渲染效果，含脚本的内容也能运行
  - Twitter/X 嵌入：自动识别 Twitter/X 链接，支持卡片模式嵌入
  - 拖拽调整：嵌入区域高度可拖拽调整
  - 网站图标：网址嵌入时自动显示网站图标和域名
  - 登录保持：嵌入网页的登录状态可以保持，无需重复登录

## 3.5.1

### Minor Changes

- 侧边栏 (lets-roam-sidebar)：新增块内容预览模式
  - 新增"展开内容/收起内容"功能，支持预览块内容（前几行可滚动）或展开完整内容
  - 工具栏新增展开/收起内容按钮（chevrons 图标）
  - 每个块的面包屑区域新增展开/收起按钮
  - 新块默认展开完整内容
- 侧边栏：优化工具栏布局
  - 工具栏按钮改为纯图标+tooltip，更紧凑
  - 新增"全部显示/全部隐藏"按钮（眼睛图标），控制块可见性
  - 新增"展开内容/收起内容"按钮（chevrons 图标），控制内容预览
- 侧边栏：优化标签页可见性
  - 标签栏在有内容时自动显示，空状态时隐藏
  - 新建标签按钮移至标签栏内

## 3.5.0

### Features

- 侧边栏 (lets-roam-sidebar)：新增多标签页支持
  - 支持创建、切换、删除、复制、重命名标签页
  - 标签页名称可自定义
  - 支持拖拽排序标签页
  - 从编辑器拖拽块到侧边栏时，支持插入到指定位置（插入线引导）
  - 默认追加到当前标签底部
- 侧边栏：新增清空当前标签功能
  - 工具栏增加清空按钮，点击后弹出确认对话框
  - 一键移除当前标签中所有堆叠块，不删除原始块
- 侧边栏：新增块搜索功能
  - 工具栏增加搜索图标，点击展开搜索输入框
  - 实时过滤当前标签中的块，匹配块文本内容
  - 支持 Escape 清空搜索，搜索时自动禁用拖拽操作
  - 切换标签时自动清空搜索状态

## 3.4.0

### Features

- 侧边栏 (lets-roam-sidebar)：新增堆叠块拖拽排序
  - 鼠标悬停时显示拖拽手柄，支持上下拖拽调整块的排列顺序
  - 拖拽过程中显示清晰的位置插入线，精准反馈释放后的目标位置
  - 拖拽到自身位置或相邻位置时自动跳过，避免无效移动

### Improvements

- 格式化块：优化文本片段格式化逻辑，当文本片段与非文本片段（例如引用链接）相邻且带有空格时，保留该空格以维持排版美观。

### Bug Fixes

- 侧边栏：修复了侧边栏（lets-roam-sidebar）在初始化加载时由于数据未拉取完成导致的状态丢失与同步竞态问题，确保块的拖入和删除能够被正确、持久地保存。
- 侧边栏：修复 moveStackedBlock 在向下拖拽时因 splice 后数组左移导致的 off-by-one 位置偏差

### Minor Changes

- fb10012: - 🔗 收集引用 (Gather Refs): 新增收集反向引用子插件
  - 支持通过斜杠命令 `/gather` 触发
  - 自动聚合引用了当前聚焦块的所有顶级节点（Page/Card），并去重
  - 使用纯文本 Markdown 语法 `[Title](orca-note://...)` 批量插入引用，避免反向链接面板污染
  - 自动在光标位置生成整洁美观的 MOC 目录树
- ad99c64: - 🔃 块排序 (Sort Blocks): 全新升级多预设配置及现代化 UI
  - 多预设配置：支持在设置中创建无限个自定义排序预设（Profiles）。
  - 原生规则调序：引入全新的无依赖轻量级 UI，支持通过上下箭头对不同类型块（任务、标题、媒体、空白块等）的排序优先级进行灵活组合。
  - 智能菜单交互：如果只保留一个预设，右键一键即可秒速排序；如果有多个预设，右键菜单会自动变为子菜单悬浮展开供你选择具体的排序规则。
  - 向下兼容：旧版单一配置字符串将会在首次加载时自动无缝迁移至全新的多预设系统。

## 3.3.0

### Minor Changes

- c08ebf2: - 局部关系图谱：新增全局侧边栏局部关系图谱展示子插件
  - 支持实时跟踪编辑器导航轨迹，展示当前活动块及其足迹网络的局部拓扑结构
  - 提供对标签节点、父子大纲层级结构、常规显式引用的快捷视图菜单过滤
  - 支持右键点击边缘邻居节点，进行局部定向拓展或折叠收起
  - 局部关系图谱：新增回放探索模式
    - 点击幻灯片图标进入回放模式，足迹节点按访问顺序以 900ms 间隔逐步呈现
    - 回放期间相机自动跟随最新节点，用户拖拽/缩放后暂停跟随
    - 退出回放时自动恢复录制状态，手动干预录制不受影响
  - 局部关系图谱：增强遍历路径可视化

## 3.2.0

### Features

- 编辑器层级折叠：新增精准控制主编辑器展示深度的子插件
  - 支持右键点击任意块，执行"展开至此同级"
  - 支持通过全局命令绑定快捷键，一键展开至第 1~5 层
- 大纲导航新增快照式层级展开功能，可一键展开至指定层级
  - 为高级用户注册了全局编辑器命令"大纲导航：展开至层级 1~5"以及"大纲导航：展开全部"
  - 优化交互反馈：大纲侧边栏底部展开按钮现支持多模式联动操作

## 3.1.1

### Improvements

- 新增大纲导航设置项：隐藏内置大纲。开启后在大纲导航面板激活时自动隐藏 Orca 内置的大纲面板，卸载时自动复原。

## 3.1.0

### Features

- 新增大纲树快捷过滤功能
  - 支持快捷点击「标题」「待办」「已办」，一键提取文档骨架与任务视图
  - 支持在搜索框输入指令查询，可与普通文本搜索组合使用
- 优化点击导航交互
  - 左键点击：编辑器平滑滚动定位到目标块
  - Alt/Option + 左键点击：编辑器下钻到该块
  - 右键点击：呼出上下文菜单，提供快捷转换等功能

## 3.0.0

### New Features

- 新增大纲导航

### Improvements

- 新增侧边栏配置项：大纲导航 (Block Nav) 和 Arc 侧边栏现在支持通过设置配置默认在左侧还是右侧打开
- 新增侧边栏快捷操作：右键点击侧边栏图标，或按住 Shift 点击，可以在屏幕反方向立刻唤出面板

## v2.12.0

### New Features

- 新增链接工具 (Link Tools)：右键块链接/引用时支持快捷转换
- 新增右键菜单注入：提取元数据、在网页视图打开
- 新增「关于」页面：展示插件信息、相关链接、更新日志

### Improvements

- 优化更新说明：使用弹窗替代外部链接

### Bug Fixes

- 修复 emoji 截断问题：转换为文本 📌 时正确处理多字节字符

## v2.11.0

### New Features

- 新增 Arc 侧边栏 (Arc Tabs)：类 Arc 浏览器的标签页管理
- 新增侧边栏收集箱 (Roam Sidebar)：全局唯一的收集容器
- 新增置顶块面板：支持网格/列表双布局

### Improvements

- 优化块工具箱：新增归集子块功能

## v2.9.0

### New Features

- 新增 Arc 侧边栏初始版本
- 新增 Roam 侧边栏初始版本

## v2.7.0

### New Features

- 新增块流转 (Block Flow)：快速将块发送到指定位置

## v2.6.0

### New Features

- 新增隐私块 (Privacy Block)：保护敏感内容

## v2.5.0

### New Features

- 新增块工具箱 (Block Tools)：增强块右键菜单

## v2.4.0

### New Features

- 新增智能剪贴板注入：支持 JSON 解析为虎鲸块

## v2.3.0

### New Features

- 新增随机漫步 (Random Walk)：基于标签的知识探索

## v2.2.0

### New Features

## v2.1.0

### New Features

- 新增块嵌入子项支持 (Embed Children)

## v2.0.0

### New Features

- 新增记忆复习 (SRS)：基于 FSRS 算法的间隔重复

## v1.x

### Initial Release

- 一键格式化 (Format Block)
- VoiceNotes 同步
- 导入工具 (Import)
- 样式清除 (Remove Style)
- 块排序 (Sort Blocks)
- 发布到 GitHub (Publish)
- 插件市集 (Bazaar)
- 快捷标签 (Quick Tag)
- 标题层级整理 (Heading Tree)
- 网页助手 (Web Assistant)
- 编辑器扩展命令 (Editor Commands)
